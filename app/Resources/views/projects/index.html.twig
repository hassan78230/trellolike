{% extends 'base.html.twig' %}

{% block body %}
    <h1>Projects list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in app.user.teams %}

          {% for project in team.getProjects %}
            <tr>
                <td><a href="{{ path('projects_show', { 'id': project.id }) }}">{{ project.id }}</a></td>
                <td>{{ project.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('projects_show', { 'id': project.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('projects_edit', { 'id': project.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
            {% endfor %}
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projects_new') }}">Create a new project</a>
        </li>
    </ul>
{% endblock %}
